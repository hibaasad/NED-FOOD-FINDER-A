<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.nedfoodfinder">

    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the 'MyLocation' functionality.
    -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-feature android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:launchMode="singleTop"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/CustomTheme">

        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".MapsActivity"
            android:label="@string/title_activity_maps"></activity>
        <activity
            android:name=".TotalUserActivity"
            android:label="Total Users" />
        <activity
            android:name=".CartActivity"
            android:label="Cart" />
        <activity
            android:name=".MechActivity"
            android:label="MechCorner Canteen" />
        <activity
            android:name=".CanteensActivity"
            android:label="Canteen" />
        <activity
            android:name=".SFCActivity"
            android:label="SFC Canteen" />
        <activity android:name=".AdminpanelActivity" />
        <activity
            android:name=".CustomerpanelActivity"
            android:label="Customer Panel"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".check" />
        <activity
            android:name=".loginActivity"
            android:theme="@style/splash" />
        <activity
            android:name=".WelcomeActivity"
            android:theme="@style/splash" />
        <activity
            android:name=".VerifyPhoneActivity"
            android:theme="@style/splash" />
        <activity
            android:name=".MainActivity"
            android:theme="@style/splash">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>